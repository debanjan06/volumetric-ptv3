# Your existing model configuration parameters (channels, layers, etc.)
model:
  input_channels: 14
  hidden_channels: 128
  num_classes: 16

# Append these exact keys for the Qualcomm Cloud AI 100 pipeline
hardware_compilation:
  target_platform: "Qualcomm Cloud AI 100"
  opset_version: 16
  optimization_level: "O3"
  static_shape:
    batch_size: 1
    point_count: 8192
    feature_dimensions: 14